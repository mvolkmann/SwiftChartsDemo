import Charts
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            HeartChartView()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
